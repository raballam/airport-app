export const assertEquals = (actual, expected) => actual === expected;
export const assertTrue = (actual) => actual === true;
export const assertFalse = (actual) => actual === false;
export const assertError = (actual) => actual instanceof Error;